[Unit]
Description=Run Xserver Backup (0,9,12,15,18,21)

[Timer]
# 指定した時間の00分に実行
OnCalendar=00,09,12,15,18,21:00:00
# PCがスリープなどで実行時間を過ぎていた場合、起動直後に実行する
Persistent=true
Unit=xsrv-backup.service

[Install]
WantedBy=timers.target

